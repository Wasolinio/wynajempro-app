import { initializeApp } from "firebase/app";
import { getFirestore, doc, updateDoc } from "firebase/firestore";

const firebaseConfig = {
  // Wait, I don't need the real config since I can use the one from src/firebase.js
};
