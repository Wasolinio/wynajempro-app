# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sekrety / zmienne środowiskowe — NIGDY nie commituj prawdziwych sekretów
.env
.env.*
!.env.example
!*.env.example
# klucze serwisowe Firebase/GCP (pełny dostęp do projektu)
*service-account*.json
*serviceAccount*.json
*firebase-adminsdk*.json

# Firebase
.firebase/
firebase-debug.log*

# ESLint
.eslintcache

# AI Agents workspace
.agents/
.claude/worktrees/
.codex/
PROJECT.md
# humanizer to osobne repo git (klon) — nie zagnieżdżamy repozytoriów
.claude/skills/humanizer/

# lokalny cache npm — nigdy do repo (raz trafił: 2337 plików, wyczyszczone 2026-08-27)
.npm_cache/

# Obsidian otwierany w korzeniu projektu (właściwy vault: docs/)
/.obsidian/

# Playwright
test-results/
playwright-report/

# Obsidian — osobisty układ paneli/grafu (szum, nie do repo; konfiguracja vaulta zostaje)
docs/.obsidian/workspace*.json
docs/.obsidian/graph.json
docs/.obsidian/cache

# czytelne kopie dokumentów (generowane: npm run docs:docx)
docs/docx/

# Plik tymczasowy generatora banerów (scripts/build-social-banners.mjs) — zostaje
# w _design-reference tylko po twardym przerwaniu procesu; nie commitować.
_design-reference/design_handoff_identyfikacja_v2/.__eksport-tmp.dc.html
